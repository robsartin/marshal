<!-- What does this change do, and why? Link the issue it addresses (e.g. "Closes #12"). -->

## What & why

## Checklist

- [ ] `./gradlew clean check` is green (Spotless, tests with `-ea`, JaCoCo line > 80% / branch > 65%, ArchUnit)
- [ ] Tests cover the change (TDD where it applies)
- [ ] Added an ADR under `docs/adr/` if this is an architecturally significant decision (and superseded rather than edited any existing one)
- [ ] Updated developer/user docs (`README`, `docs/dev/`) if behavior or the public API changed
